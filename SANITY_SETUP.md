# Установка и настройка Sanity CMS

## ✅ Частично выполнено автоматически:

Sanity CLI уже создал проект и настроил базовую конфигурацию:
- ✅ Проект Sanity создан (ID: 9qjrff3g)
- ✅ Переменные окружения настроены в `.env.local`
- ✅ Базовая структура файлов создана
- ✅ Схемы данных добавлены в `src/sanity/schemaTypes/`

## 📋 Осталось выполнить:

### Шаг 1: Установка зависимостей

Выполните в терминале:

```bash
cd C:\xampp\htdocs\ebike-rent
npm install --legacy-peer-deps
```

Если возникнут ошибки, попробуйте:

```bash
npm install --force
```

### Шаг 2: Запуск Next.js проекта

```bash
npm run dev
```

### Шаг 3: Запуск Sanity Studio

Sanity Studio интегрирована в Next.js приложение. 

1. Запустите Next.js (если еще не запущен):
```bash
npm run dev
```

2. Откройте админ-панель по адресу:
```
http://localhost:3000/studio
```

Примечание: Sanity Studio работает внутри Next.js на порту 3000, а не на отдельном порту 3333.

### Шаг 4: Создание контента в Sanity Studio

В Sanity Studio создайте:

1. **Hero секция** (Главная страница → Hero секция)
   - Заголовок: "Аренда электровелосипедов для курьеров в Красноярске"
   - Подзаголовок: "Доставляй быстрее. Зарабатывай больше."
   - Бейдж: "Для Курьеров"
   - Статистика 1: 50+ км, "км без подзарядки"
   - Статистика 2: 2×, "больше заказов"
   - Статистика 3: 3, "тарифа аренды"
   - Кнопки: "Выбрать тариф" → #tariffs, "Смотреть каталог" → #catalog

2. **Тарифы** (Главная страница → Тарифы)
   - Базовый: 3000 ₽/неделя, 2 АКБ × 21 Ah
   - Стандарт: 3400 ₽/неделя, 2 АКБ × 30 Ah (популярный)
   - Премиум: 3700 ₽/неделя, 2 АКБ × 30 Ah + комфорт

3. **Отзывы** (Главная страница → Отзывы)
   - Добавьте 3-4 отзыва курьеров

4. **FAQ** (Главная страница → FAQ)
   - Добавьте 5-7 вопросов и ответов

5. **Велосипеды** (Велосипеды)
   - Добавьте модели велосипедов с изображениями

### Шаг 5: Интеграция с Next.js

После создания контента сообщите мне, и я подключу Sanity данные к компонентам сайта.

## 📁 Созданные файлы:

- `sanity.config.ts` - конфигурация Sanity Studio
- `src/sanity/schemaTypes/` - схемы данных (hero, tariff, review, faq, bike)
- `src/sanity/structure.ts` - структура админ-панели
- `src/sanity/env.ts` - переменные окружения
- `src/lib/sanity.ts` - клиент Sanity
- `src/lib/sanityQueries.ts` - функции для получения данных
- `.env.local` - переменные окружения (уже настроены)

## 🎯 Преимущества Sanity CMS:

✅ **Бесплатный тариф**: 3 пользователя, неограниченный контент  
✅ **Стабильная работа**: облачная инфраструктура 99.9% uptime  
✅ **Понятный интерфейс**: современный редактор с real-time обновлениями  
✅ **Мощное API**: быстрое получение контента через CDN  
✅ **Гибкая структура**: настройка схем данных под ваши нужды  
✅ **Image optimization**: автоматическая оптимизация изображений  
✅ **Preview mode**: просмотр изменений перед публикацией